import { moduleForComponent, /*test*/ } from 'ember-qunit';
// import hbs from 'htmlbars-inline-precompile';
// import { generateInvoices } from 'ember-invoice-manager/tests/helpers/generate-invoices';

moduleForComponent('invoice-table', 'Integration | Component | invoice table', {
  integration: true
});

// generateInvoices(server, 5, 5);

// test('it renders 5 elements', function(assert) {

//   this.render(hbs`
//   {{invoice-table 
//     model=invoiceModel}}
//   `);

//   assert.equal(this.$('.table .invoice-row').length, 5);
// });
